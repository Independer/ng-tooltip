import { Component } from '@angular/core';

@Component({
  selector: 'ind-tooltip-container',
  template: `<ng-template #tooltipContainer></ng-template>`
})
export class TooltipContainerComponent {
  // DEPRECATED: can be removed in next update
}
